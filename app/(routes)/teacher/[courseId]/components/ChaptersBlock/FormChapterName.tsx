import { FormChapterNameProps } from "./FormChapterName.types"

const FormChapterName = (props: FormChapterNameProps) => {
  const { setShowInputChapter, idCourse } = props
  return (
    <div>FormChapterName</div>
  )
}

export default FormChapterName
